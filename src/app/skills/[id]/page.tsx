import { PageContainer } from "@/components/layout/page-container";import { EmptyState } from "@/components/ui/empty-state";import { getSkill } from "@/data/mock/skillswap";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const skill=getSkill(id);return <PageContainer>{skill?<><p className="text-sm font-bold uppercase text-[var(--primary)]">Skill detail</p><h1 className="font-display text-6xl">{skill.name}</h1><p className="lede">{skill.description}</p></>:<EmptyState title="Skill not found" body="This route is ready for future skill detail data."/>}</PageContainer>}
