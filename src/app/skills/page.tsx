import { PageContainer } from "@/components/layout/page-container";import { PageHeader } from "@/components/layout/page-header";import { SkillCard } from "@/components/shared/skill-card";import { skills } from "@/data/mock/skillswap";export default function Page(){return <PageContainer><PageHeader eyebrow="Skills" title="Skill library" body="Reusable skill taxonomy and cards backed by mock data."/><div className="grid md:grid-cols-4">{skills.map(s=><SkillCard key={s.id} skill={s}/>)}</div></PageContainer>}
