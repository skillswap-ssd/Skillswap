import { PageContainer } from "@/components/layout/page-container";import { PageHeader } from "@/components/layout/page-header";import { Button } from "@/components/ui/button";import { SkillCard } from "@/components/shared/skill-card";import { categories, skills } from "@/data/mock/skillswap";
export default function Page(){return <PageContainer><PageHeader eyebrow="Skills" title="Manage what you offer and want." body="The skill library distinguishes teachable offers from learning requests while staying backed by mock data."/><Button href="/skills/new">Add skill</Button><h2 className="mt-10 font-display text-4xl">Skills I offer</h2><div className="mt-4 grid md:grid-cols-3">{skills.slice(0,3).map(s=><SkillCard key={s.id} skill={s} mode="offer"/>)}</div><h2 className="mt-10 font-display text-4xl">Skills I want</h2><div className="mt-4 grid md:grid-cols-3">{skills.slice(3,6).map(s=><SkillCard key={s.id} skill={s} mode="want"/>)}</div><h2 className="mt-10 font-display text-4xl">Category system</h2><div className="mt-4 grid md:grid-cols-5">{categories.map(c=><article className="border border-[var(--border)] bg-[var(--surface)] p-4" key={c.id}><b>{c.name}</b><p className="text-sm text-[var(--muted)]">{c.description}</p></article>)}</div></PageContainer>}
