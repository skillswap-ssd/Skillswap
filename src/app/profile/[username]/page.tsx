import { PageContainer } from "@/components/layout/page-container";import { EmptyState } from "@/components/ui/empty-state";import { Avatar } from "@/components/ui/avatar";import { profiles, getUser } from "@/data/mock/skillswap";export default async function Page({params}:{params:Promise<{username:string}>}){const {username}=await params;const user=getUser(username);const profile=profiles.find(p=>p.userId===user?.id);return <PageContainer>{user&&profile?<section className="editorial"><div><Avatar name={user.name}/><h1 className="font-display text-6xl">{user.name}</h1><p className="lede">{profile.headline}</p><p>{profile.bio}</p></div></section>:<EmptyState title="Profile not found" body="Profile routes are wired for future backend data."/>}</PageContainer>}
