import { PageContainer } from "@/components/layout/page-container";import { PageHeader } from "@/components/layout/page-header";import { Toast } from "@/components/ui/toast";import { notifications } from "@/data/mock/skillswap";export default function Page(){return <PageContainer><PageHeader eyebrow="Notifications" title="Signals, not noise." body="A primitive notification surface prepared for matches, messages, reviews, and community updates."/>{notifications.map(n=><Toast key={n.id} message={n.title}/>)}</PageContainer>}
