import Link from "next/link";import { PageContainer } from "@/components/layout/page-container";import { PageHeader } from "@/components/layout/page-header";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";import { Card } from "@/components/ui/card";
export default function Page(){return <PageContainer><section className="editorial"><div><PageHeader eyebrow="Mock authentication" title="Welcome back." body="Log in to preview the authenticated SkillSwap experience. This is front-end state only, not secure authentication."/><p className="lede">Use any email and password; later phases can connect these forms to real auth.</p></div><Card><form className="grid gap-4"><Input aria-label="Email" type="email" placeholder="Email"/><Input aria-label="Password" type="password" placeholder="Password"/><label className="flex items-center gap-2 text-sm"><input type="checkbox"/> Remember this device</label><Button href="/">Log in</Button><a className="text-sm font-bold text-[var(--primary)]" href="#">Forgot password?</a><p className="text-sm">New here? <Link className="font-bold" href="/signup">Create an account</Link></p><div className="border-t border-[var(--border)] pt-4 text-sm text-[var(--muted)]">Social login placeholders: Google · GitHub</div></form></Card></section></PageContainer>}
