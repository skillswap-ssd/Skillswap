import { PageContainer } from "@/components/layout/page-container";import { PageHeader } from "@/components/layout/page-header";import { Dialog } from "@/components/ui/dialog";export default function Page(){return <PageContainer><PageHeader eyebrow="Messages" title="Conversations will live here." body="The shell reserves space for future peer-to-peer coordination without implementing messaging logic yet."/><Dialog title="No conversations yet"><p className="lede">Start with discovery, then future matches can open conversations.</p></Dialog></PageContainer>}
