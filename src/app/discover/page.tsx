import { PageContainer } from "@/components/layout/page-container";import { PageHeader } from "@/components/layout/page-header";import { SearchInput } from "@/components/shared/search-input";import { FilterButton } from "@/components/shared/filter-button";import { SkillCard } from "@/components/shared/skill-card";import { skills } from "@/data/mock/skillswap";export default function Page(){return <PageContainer><PageHeader eyebrow="Discover" title="Find your next exchange." body="A preview of the discovery surface future matching and recommendations will use."/><SearchInput/><div className="my-5 flex flex-wrap gap-2"><FilterButton>Remote</FilterButton><FilterButton>Beginner friendly</FilterButton><FilterButton>High reputation</FilterButton></div><div className="grid md:grid-cols-3">{skills.slice(0,6).map(s=><SkillCard key={s.id} skill={s}/>)}</div></PageContainer>}
