import { PageContainer } from "@/components/layout/page-container";import { PageHeader } from "@/components/layout/page-header";import { SearchInput } from "@/components/shared/search-input";import { FilterButton } from "@/components/shared/filter-button";import { SkillCard } from "@/components/shared/skill-card";import { UserCard } from "@/components/shared/user-card";import { EmptyState } from "@/components/ui/empty-state";import { categories, profiles, skills, users } from "@/data/mock/skillswap";
export default function Page(){return <PageContainer><PageHeader eyebrow="Discover" title="Find people, skills, and mutual value." body="Search and filters are local mock filtering surfaces designed for a future API."/><SearchInput/><div className="my-5 flex gap-2 overflow-x-auto pb-2 md:flex-wrap"><FilterButton>Technology</FilterButton><FilterButton>Beginner friendly</FilterButton><FilterButton>Remote</FilterButton><FilterButton>High reputation</FilterButton><FilterButton>Available this week</FilterButton></div><section className="grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><div><h2 className="font-display text-4xl">Potential exchanges</h2>{users.map((u,i)=><UserCard key={u.id} user={u} headline={profiles[i].headline} match={i===0?"You want Python; Mara wants it too, while offering photography context.":i===2?"Design ↔ data is a promising practical trade.":undefined}/>)}</div><div><h2 className="font-display text-4xl">Skills in motion</h2><div className="mt-4 grid gap-4">{skills.slice(0,5).map(s=><SkillCard key={s.id} skill={s}/>)}</div></div></section><section className="mt-10"><h2 className="font-display text-4xl">Browse categories</h2><div className="mt-4 grid md:grid-cols-5">{categories.map(c=><div className="border-t border-[var(--border)] py-4" key={c.id}><b>{c.name}</b><p className="text-sm text-[var(--muted)]">{c.description}</p></div>)}</div></section><div className="mt-8"><EmptyState title="No results yet?" body="Clear search, broaden filters, or add the skill you want so future matching has something to work with."/></div></PageContainer>}
