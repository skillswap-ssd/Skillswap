import Link from "next/link";import { cn } from "@/lib/utils";
type Props=React.ButtonHTMLAttributes<HTMLButtonElement>&{variant?:"primary"|"secondary"|"ghost";href?:string};
export function Button({className,variant="primary",href,children,...props}:Props){const cls=cn("focus-ring inline-flex min-h-11 items-center justify-center border px-5 py-2.5 text-sm font-bold transition hover:-translate-y-0.5",variant==="primary"&&"bg-[var(--primary)] text-[var(--primary-foreground)] border-[var(--primary)]",variant==="secondary"&&"bg-[var(--surface)] text-[var(--foreground)] border-[var(--border)]",variant==="ghost"&&"border-transparent hover:border-[var(--border)]",className);return href?<Link className={cls} href={href}>{children}</Link>:<button className={cls} {...props}>{children}</button>}
