import type { User } from "@/data/models";import { Avatar } from "@/components/ui/avatar";import { Rating } from "./rating";export function UserCard({user,headline}:{user:User;headline:string}){return <article className="flex gap-4 border-t border-[var(--border)] py-5"><Avatar name={user.name}/><div><h3 className="font-bold">{user.name} <span className="text-[var(--muted)]">@{user.username}</span></h3><p>{headline}</p><Rating value={user.reputation}/></div></article>}
