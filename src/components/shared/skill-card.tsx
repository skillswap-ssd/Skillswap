import Link from "next/link";import type { Skill } from "@/data/models";import { Card } from "@/components/ui/card";import { Badge } from "@/components/ui/badge";import { CategoryChip } from "./category-chip";import { categoryName, toneFor } from "@/data/mock/skillswap";
export function SkillCard({skill,mode="discover"}:{skill:Skill;mode?:"offer"|"want"|"discover"}){return <Link href={`/skills/${skill.id}`}><Card className="group relative min-h-64 overflow-hidden transition hover:-translate-y-1"><div className="absolute right-0 top-0 h-20 w-20 bg-[var(--primary)] opacity-10"/><CategoryChip tone={toneFor(skill.categoryId)}>{categoryName(skill.categoryId)}</CategoryChip><h3 className="font-display mt-5 text-3xl leading-none">{skill.name}</h3><p className="lede text-base">{skill.description}</p><div className="mt-4 flex flex-wrap gap-2"><Badge>{skill.level}</Badge><Badge>{skill.popularity} people</Badge></div><div className="mt-5 flex flex-wrap gap-2 text-xs font-bold text-[var(--muted)]">{skill.tags.slice(0,3).map(t=><span key={t}>#{t}</span>)}</div><p className="mt-5 font-bold text-[var(--primary)]">{mode==="offer"?"Manage offer":mode==="want"?"Manage request":"Explore exchange →"}</p></Card></Link>}
